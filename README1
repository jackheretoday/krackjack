Hello World

q1.AWS & putty connection
1.create instance allow ssh connection in inbound rules custom 0.0.0/0 & download the security key in .ppk
2.download putty if not available, open puttygen and load the downloaded key.
3.save private key,without password, name the file,save it.
4.open putty paste the public ip of instance, goto ssh, auth , credit, browse the saved puttygen.
5.go to session save it, open,login as:ec2-user , pwd, whoami

q2.=reffer q10. & q13.

q3.Git on aws
sudo apt update -y
sudo apt install git -y
git --version
git status
git log
if want to login git;
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
git config --list
echo "Hello Git!" > test.txt
git add test.txt
git status

to start jenkin:
net start jenkins
net stop jenkins

q4.Compile project in Jenkins using Maven
new item, freestyle, git-> repo link, master, 
build step->invoke maven target,
goal->compile , apply , save

q5.Create slave node in Jenkins
Settings, nodes, new node, name it,
permanent agent, create,create folder in c drive,
path copy, paste save, run from agent link copy paste in cmd.

q6.Assign project to slace node & build project
same steps, restrict where project can run, label selenium, 
copy paste java file in slave folder in workspace folder folder,
build steps javac hello.java  ,java hello apply save, build now.

q7.Run Selenium tests in Jenkins using Maven.
new item, maven,  git->selenium, master, build-> pom.xml, apply save.

q8. Build Pipeline project in Jenkins
Go to Dashboard → Manage Jenkins → Plugins → Available Plugins, and install:
Git Plugin
Pipeline Plugin
Blue Ocean Plugin (optional, for better UI)
Go to Jenkins Dashboard → New Item
Select “Pipeline”
Enter a name, e.g. hello-world-pipeline
Click OK
go to pipeline script type: print("Hello World")
apply, save, check the build, go to console output

q9.GIT operations on local & remote repositories
git init
git add .
git commit -m "Initial version"
git branch -M main
git remote add origin https://github.com/jackheretoday/DevOps_labs.git
git push -u origin main
git pull origin main
git clone https://github.com/<username>/<repo-name>.git

q10.Pull ubuntu image on docker & execute bash
1.create instance on EC2 as ubuntu
2.connect to instance & run commands:
(q2.)sudo apt-get update && sudo apt-get upgrade
sudo apt-get install docker.io -y
sudo systemctl enable docker
docker --version //q2
sudo docker info
sudo docker images
sudo docker pull ubuntu
sudo docker images
sudo docker run -it -d ubuntu
sudo docker ps
sudo docker ps -a
sudo docker exec -it <container_id> bash
pwd
mkdir anyfolder
ls
exit
sudo docker run hello-world #doubtful
sudo docker pull alpine
sudo docker images

q11.git pull make changes then push.
create project file, open in vscode
sudo apt update -y
sudo apt install git -y
git config --global user.name "Your Name"
git config --global user.email "your-email@example.com"
git clone https://github.com/<username>/<repository>.git
cd <repository>
nano index.html
git add .
git commit -m "Updated index.html"
git push origin main
verify the changes on github repo.

q12.pull docker images,list the docker images currently stored on your machine, remove an image that you no longer need
1.create instance on EC2 as ubuntu
2.sudo apt-get update && sudo apt-get upgrade
sudo apt-get install docker.io -y
sudo systemctl enable docker
docker --version
sudo docker info
sudo docker images
sudo docker pull ubuntu
sudo docker pull alpine
sudo docker images
#to delete specific image
sudo docker rmi alpine:latest

q13.basic docker commands
q2.same then...
#List running containers
sudo docker ps
# List all containers (including stopped)
sudo docker ps -a
# Start stopped container
docker start container_name
# Stop container
docker stop container_name
# Restart container
docker restart container_name
# Remove container
docker rm container_name
# Remove running container (force)
docker rm -f container_name

q14.git commands from project cmd & q15.meaningful message push to remote repo 
1.Go to your project folder
bash
cd path/to/project
2. Initialize Git repository
bash
git init
This creates a .git folder and starts version control.
3. Create a new file
bash
nano demo.txt
(or touch demo.txt if you don’t want to open editor)
Write anything → save & exit.
4. Add the file to staging area
bash
git add demo.txt
(or add everything → git add .)
5. Commit the file with a message
bash
git commit -m "Added demo.txt file as initial commit"
6. Add remote GitHub repo
bash
git remote add origin https://github.com/your-username/your-repo.git

Check if remote is added:
bash
git remote -v
7. Push changes to GitHub
If branch is *master*:
bash
git push -u origin master

If branch is *main*:
bash
git push -u origin main
Final Output
Initialized empty Git repository
[master (root-commit) 1a2b3c4] Added demo.txt file...
Important Notes
✔ GitHub now *rejects password login* → use *PAT (Personal Access Token)*
✔ First push needs -u to set upstream
✔ If repo already has files, git pull before push

q16.setup ci pipeline using jenkins, configure the pipeline to build a simple project("Hello World application)
Step 1: Create a Simple Hello World Project
Example: hello.java
java
public class hello {
    public static void main(String[] args) {
      	 System.out.println("Hello World from Jenkins!");
  	 }
}

Step 2: Create a GitHub Repo & Push Code
bash
git init
git add .
git commit m "Hello World project added"
git remote add origin https://github.com/username/jenkinsdemo.git
git push u origin master

Step 3: Install Required Jenkins Plugins
Go to:
Jenkins → Manage Jenkins → Manage Plugins → Available
Git plugin
Pipeline plugin
Maven Integration (if Java project)
GitHub Integration plugin

Step 4: Create a New Pipeline Job
1. Go to Jenkins Dashboard
2. Click New Item
3. Enter job name → helloworldpipeline
4. Select Pipeline → OK

Step 5: Add Pipeline Script (Jenkinsfile)
Inside Jenkins → Pipeline → Paste:
groovy
pipeline {
  	 agent any
  	  
  	 stages {
  	 	 	 stage('Clone Repo') {
  	 	 	 	 	 steps {
  	 	 	 	 	 	 	 git 'https://github.com/username/jenkinsdemo.git'
  	 	 	 	 	 }
  	 	 	 }
  	 	 	 stage('Build') {
  	 	 	 	 	 steps {
  	 	 	 	 	 	 	 sh 'javac hello.java'
  	 	 	 	 	 }
  	 	 	 }
  	 	 	 stage('Run') {
  	 	 	 	 	 steps {
  	 	 	 	 	 	 	 sh 'java hello'
  	 	 	 	 	 }
  	 	 	 }
  	 }
}
If you're using Windows, replace sh with bat.

Step 6: Run the Pipeline
Click Build Now
Console Output should show:
Hello World from Jenkins!
```

q17. docker container, image website hosting.
1.create ec2 instance, 
Click on the Security tab
Click on the Security Group link (e.g., sg-xxxxxxxxx)
Add Inbound Rule
Click Edit inbound rules
Click Add rule
Configure the new rule:
Type: Custom TCP
Port range: 8080
Source: 0.0.0.0/0 (for public access) or Your IP (for restricted access)
Description: Docker HTML App
Click Save rules
connect
2.sudo apt update && sudo apt upgrade -y
sudo apt install docker.io -y
sudo systemctl start docker
sudo systemctl enable docker
mkdir ~/myhtmlapp
cd ~/myhtmlapp
vim index.html
type basic html
cd ..
vim Dockerfile 
paste: 
# Use the official Nginx image
FROM nginx:alpine

# Copy the HTML file to the Nginx web directory
COPY myhtmlapp/index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80

3.
ls -la
sudo docker images
sudo docker run -d -p 8080:80 --name my-htmlapp htmlapp:latest
sudo docker ps
curl http://localhost:8080






